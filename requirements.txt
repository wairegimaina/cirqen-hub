# CIRQEN Website — Python Requirements
# Generated for: app.py (Flask web server)
# Python version: 3.10+
#
# Install with:
#   pip install -r requirements.txt

# Core framework
Flask==3.1.3

# Flask dependencies (auto-installed, pinned for reproducibility)
Werkzeug==3.1.7
Jinja2==3.1.6
click==8.3.1
itsdangerous==2.2.0
MarkupSafe==3.0.3
blinker==1.9.0
